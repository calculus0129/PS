#include <bits/stdc++.h>
using namespace std;
int main() {
    cin.tie(NULL); ios_base::sync_with_stdio(false);
    int n; cin >> n;
    int s = sqrt(n);
    cout << "The largest square has side length " << s << '.';
    return 0;
}