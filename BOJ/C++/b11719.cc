#include <bits/stdc++.h>
using namespace std;
int main() {
    cin.tie(NULL); ios_base::sync_with_stdio(false);
    char ch;
    while((ch = cin.get()) != EOF) {
        cout << ch;
    }
    return 0;
}