#include <bits/stdc++.h>
#define X first
#define Y second
using namespace std;
int cheese[100][100];

int main()
{
    cin.tie(0)->sync_with_stdio(0);
    cin >> m >> n;
    for(int i=0,j,c;i<m;++i) for(j=0;j<n;++j)
    {
        cin >> c;
        cheese[i][j] = c;
        if(c==1)
        {

        }
    }
    //unfinished

    return 0;
}
