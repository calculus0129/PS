use std::io;
fn main() {
    let mut s = String::new();
    io::stdin().read_line(&mut s).unwrap();
    let mut n = s.trim().parse::<usize>();
    for _ in 0..n {
        let (s1, s2) = (String::new(), String::new());
        s = string::new();
        io::stdin().read_line(&mut s).unwrap();
        s.trim().split_ascii_whitespace().
        // later
    }
}
