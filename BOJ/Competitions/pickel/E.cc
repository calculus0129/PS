#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
struct Node {
	ll v;
	Node* l, r;
	Node(ll v=0LL, l=NULL, r=NULL) : v(v), l(l), r(r) {}
};

struct Seg {
	Node *root;
	Seg() {
		root = new Node();
	}
	void 
};

int main() {
	cin.tie(NULL); ios_base::sync_with_stdio(false);
	return 0;
}
